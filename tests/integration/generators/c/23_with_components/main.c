#include <stdio.h>

#include "sample_module.h"

int main() {
  /* TODO

  _Static_assert(
    sizeof(Sample_packet) == 24,
    "The size of the sample_packet shall be 24 bytes."
  );

  With_component_packet with_component_packet = sample_packet.sample_packet_with_component;

  _Static_assert(
    with_component_packet.with_component_packet_number_1 == 23,
    ""
  );
  _Static_assert(
    with_component_packet.with_component_packet_number_2 == 42,
    ""
  );

  Inner_with_component_packet inner_with_component_packet = with_component_packet.with_component_packet_inner_with_component;

  _Static_assert(
    inner_with_component_packet.inner_with_component_packet_number_1 == 1,
    ""
  );
  _Static_assert(
    inner_with_component_packet.inner_with_component_packet_number_2 == 9,
    ""
  );

  _Static_assert(
    with_component_packet.with_component_packet_bool == 1,
    ""
  );
  */
  return 0;
}